[package]
name = "omnesagent-commands"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared built-in slash command catalogue for OmnesAgent surfaces."
publish = false

[dependencies]
serde = { version = "1.0", default-features = false, features = ["derive"] }
