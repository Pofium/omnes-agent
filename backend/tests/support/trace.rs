//! JSON trace fixture types — re-exported from the shipped `omnesagent-eval` crate.

pub use omnesagent_eval::case::{
    LlmTrace, TraceExpects, TraceResponse, TraceStep, TraceToolCall, TraceTurn,
};
