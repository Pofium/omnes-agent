# Development Environment for OmnesAgent Agentic Testing
#
# Use this for:
# - Running the agent in a sandboxed environment
# - Testing dangerous commands safely
# - Developing new skills/integrations
#
# Usage:
#   cd dev && ./cli.sh up
#   or from root: ./dev/cli.sh up
name: omnesagent-dev
services:
  # ── The Agent (Development Image) ──
  # Builds from source using the 'dev' stage of the root Dockerfile
  omnesagent-dev:
    build:
      context: ..
      dockerfile: Dockerfile
      target: dev
    container_name: omnesagent-dev
    restart: unless-stopped
    environment:
      - OMNESAGENT_GATEWAY_PORT=42617
      - SANDBOX_HOST=omnesagent-sandbox
    secrets:
      - source: omnesagent_env
        target: omnesagent_env
    entrypoint: ["/bin/bash", "-lc"]
    command:
      - |
        if [ -f /run/secrets/omnesagent_env ]; then
          set -a
          . /run/secrets/omnesagent_env
          set +a
        fi
        exec omnesagent gateway start --port "${OMNESAGENT_GATEWAY_PORT:-42617}" --host "[::]"
    volumes:
      # Mount single config file (avoids shadowing other files in .omnesagent)
      - ../target/.omnesagent/config.toml:/omnesagent-data/.omnesagent/config.toml
      # Mount shared workspace
      - ../playground:/omnesagent-data/workspace
    ports:
      - "127.0.0.1:42617:42617"
    networks:
      - dev-net

  # ── The Sandbox (Ubuntu Environment) ──
  # A fully loaded Ubuntu environment for the agent to play in.
  sandbox:
    build:
      context: sandbox # Context relative to dev/
      dockerfile: Dockerfile
    container_name: omnesagent-sandbox
    hostname: dev-box
    command: ["tail", "-f", "/dev/null"]
    working_dir: /home/developer/workspace
    user: developer
    environment:
      - TERM=xterm-256color
      - SHELL=/bin/bash
    volumes:
      - ../playground:/home/developer/workspace # Mount local playground
    networks:
      - dev-net

networks:
  dev-net:
    driver: bridge

secrets:
  omnesagent_env:
    file: ../.env
